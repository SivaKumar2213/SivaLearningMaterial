package packages;
import java.lang.*;
import OOPS.methodOverloading;

//*- is used to get all the files not the folder
//java.lang.*- it is used get all the files present inside the lang folder
//java.*--> we will get compile time error
//If class is declared as Public it can be import in another package or else we cannot import
//in this we imported OOPS.methodOverloading it will work because methodOverloading is Public but in the same file 
//we are having an another class name Add which no modifier is specified so automatically it will consider as (default)
//so we cannot import
public class packageDetails {

	public static void main(String[] args) {
//Add c=new Add();

	}

}
